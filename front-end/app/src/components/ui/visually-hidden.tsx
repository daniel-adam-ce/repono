import * as VisuallyHiddenGlobal from '@radix-ui/react-visually-hidden';

export const VisuallyHidden = (props: VisuallyHiddenGlobal.VisuallyHiddenProps) => {
    return (
        <VisuallyHiddenGlobal.Root {...props} />
    )
}