import Styles from "./index.module.scss";
import * as Types from "./types";

export const Header = (props: any) => {
    return (
        <div
            className={Styles.header}
        >
            
        </div>
    )
}


export default { Header, Styles, Types }

